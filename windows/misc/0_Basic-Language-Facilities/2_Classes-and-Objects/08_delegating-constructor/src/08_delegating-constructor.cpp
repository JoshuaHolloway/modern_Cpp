/// Delegating Constructor
// -Allows a constructor to invoke another constructor
// -Replacement for common initialization
// -Reduces duplicate intialization code in multiple constructors.

#include "Car.h"
#include <iostream>

int main()
{
	Car car;

	getchar();
	return 0;
}